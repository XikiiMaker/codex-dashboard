#pragma once

#include "lvgl.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * 音乐控制器：迷你卡 + 全屏覆盖层
 *
 * 两套排版共用一份数据、一套刷新逻辑，差别只是控件指针和尺寸。点封面或歌名切到
 * 全屏，全屏里点封面或「返回看板」切回来。
 *
 * 迷你卡挂在竖屏 600×1024 的下半截（任务卡固定五行之后剩下的整块高度，约 420px），
 * 自上而下：状态行、分隔线、封面192 + 右侧五行文字（歌名、歌手-专辑、三行歌词）、
 * 整行进度条、控制按钮行（右端接信号阶梯式的音量条）。高度由调用方决定
 * （flex_grow=1），卡内只有封面行是弹性的，其余各段都是定高。
 *
 * parent 必须是看板的页容器：全屏覆盖层要挂到它所属的 screen 上才能盖住顶栏。
 */
void ui_music_create(lv_obj_t *parent);

#ifdef __cplusplus
}
#endif
